name: Deploy docs to Netlify on tag

on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify deploy hook
        run: curl -sf -X POST "${{ secrets.NETLIFY_DEPLOY_HOOK }}"
